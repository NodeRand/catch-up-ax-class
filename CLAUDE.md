# catch-up-ax-class

외부 강의 **「AX 직무 역량검사」** 스터디 저장소. 강의를 따라가며 실습하고, 그 과정을 필기로 남긴다.

## 저장소 구조

```
AX 직무 역량검사_강의자료/   # 강사 배포 원본 실습 자료 (0강 ~ 8강)
  ├── 0강/ ~ 8강/            # 차시별 실습 파일
  └── 결과참고/              # 차시별 정답/결과 예시 (1·2·3·4·6·7·8강)
lecture-source/              # 배포 zip 원본
docs/                        # 차시별 필기본
```

---

## 세션 역할 구분 (먼저 읽을 것)

이 저장소는 **두 종류의 세션**으로 나눠서 쓴다. 이 문서는 양쪽 다 읽으므로, 먼저 지금이 어느 쪽인지 판별하고 해당 규칙만 따른다.

| | **정리 세션** | **실습 세션** |
|---|---|---|
| 하는 일 | 강의 내용을 `docs/`에 필기로 남긴다 | 강의가 시키는 실습 작업을 실제로 수행한다 |
| 쓰는 파일 | `docs/*.md`, `CLAUDE.md` | `AX 직무 역량검사_강의자료/` 아래 실습 파일 |
| 건드리지 않는 것 | 실습 파일 (강의자료 원본) | `docs/` 필기본 |

**판별 기준** — 사용자가 세션 시작 시 어느 쪽인지 말해준다. 말이 없으면 요청 성격으로 판단한다.

- 강의 슬라이드 캡처를 붙여넣거나, "정리해줘 / 필기 / md로 남겨줘" → **정리 세션**
- 엑셀·PDF 등 실습 파일을 열거나 고치라고 함 → **실습 세션**
- 애매하면 **묻는다.** 역할을 넘겨 짚고 반대쪽 파일을 건드리지 않는다.

**역할 침범 금지.** 정리 세션에서 실습 파일을 수정하지 않고, 실습 세션에서 `docs/`를 고쳐 쓰지 않는다. 지금 역할 밖의 작업이 필요해 보이면 하지 말고, 반대쪽 세션에서 하시라고 알린다.

---

## 정리 세션 규칙

- **차시 1개 = md 파일 1개.** `docs/0강.md` ~ `docs/8강.md`. 차시가 늘지 않는 한 파일을 새로 만들지 않는다.
- 파일명은 자료 폴더명(`0강`, `1강`, …)과 그대로 맞춘다.

### 필기 내용

한 차시 md는 보통 두 덩어리로 구성된다. 완성된 예시는 [docs/1강.md](docs/1강.md) 참고.

1. **강의 개념** — 사용자가 붙여넣는 강의 슬라이드 캡처를 표/구조로 옮긴다. 슬라이드에 있는 내용만 적고, 없는 걸 지어내지 않는다.
2. **실습** — 그 차시 실습 세션에서 실제로 던진 프롬프트와 결과.

실습 내용은 사용자가 프롬프트 원문과 결과를 옮겨다 준다. 필기하는 쪽에서 직접 실습 파일을 열어 확인하지 않는다 (파일이 이후 차시에서 또 바뀔 수 있고, 그때그때의 작업 맥락은 실습 세션에만 있다).

프롬프트를 받아 적을 때:

- 문장을 다듬거나 요약하지 말고 **받아쓴 그대로** 인용 블록에 넣는다. (오타·비문 포함)
- 결과는 무엇이 몇 건 바뀌었는지 표로 짧게. 길게 해설하지 않는다.
- 사용자가 프롬프트만 불러주고 정리를 지시하지 않았다면, 그때는 프롬프트만 쌓아두고 **먼저 나서서 필기를 재구성하지 않는다.**

---

## 실습 세션 규칙

- 원본 실습 파일은 git에 커밋되어 있으므로 **제자리에서 수정**한다 (사본을 만들지 않는다).
- 요청받은 열/범위만 손댄다. 작업 중 발견한 다른 이상(중복 행, 결측치, 날짜 형식 혼재 등)은 **고치지 말고 보고만** 한다 — 강의가 이후 차시에서 다룰 소재일 수 있다.
- 수정 후에는 전수 검증하고, 무엇이 몇 건 바뀌었는지 표로 보고한다.
- 필기는 여기서 쓰지 않는다. 대신 **정리 세션에 그대로 옮겨 붙일 수 있는 형태**로 보고한다 — 사용자가 던진 프롬프트 원문 + 변경 내역 표.

---

## 커밋

- 커밋 메시지는 한국어, 앞에 이모지. 기존 예: `🎉begin: 프로젝트 시작, 0강 완료`
- 두 세션이 같은 저장소를 쓰므로, 커밋 전 `git status`로 **다른 세션이 만든 변경이 섞여 있지 않은지** 확인한다. 내 역할 범위의 파일만 담아 커밋한다.
