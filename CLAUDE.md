# catch-up-ax-class

외부 강의 **「AX 직무 역량검사」** 스터디 저장소. 강의를 따라가며 실습하고, 그 과정을 필기로 남긴다.

## 저장소 구조

```
AX 직무 역량검사_강의자료(+실습대상)/   # 강사 배포 원본 = 그대로 실습하는 대상 (0강 ~ 8강)
  ├── 0강/ ~ 8강/                       # 차시별 실습 파일
  └── 결과참고/                         # 차시별 정답/결과 예시 (1·2·3·4·6·7·8강)
lecture-source/                          # 배포 zip 원본
docs/                                    # 차시별 필기본
```

---

## 진행 방식

**한 차시 = 한 사이클.** 실습 작업과 정리 작업을 같은 세션에서 이어서 마무리한다. 둘을 다른 세션으로 나누지 않는다 — 무엇을 왜 그렇게 판단했는지는 실습을 돌린 그 자리에서만 온전히 남는다.

### 실습 작업

강의가 시키는 프롬프트를 **그대로** 넣어 실행한다. 프롬프트를 다듬거나 대신 개선하지 않는다 — 강의가 의도한 결과와 함정을 그대로 겪는 게 목적이다. 끝나면 검증하고 표로 보고한다.

### 정리 작업

사용자가 강의 중간중간의 슬라이드 캡처를 첨부하면, 그 자료와 이번 차시 실습 내용을 합쳐 필기를 마무리한다.

```
docs/N강.md 작성 → git add → 약속된 커밋 형식으로 커밋 → main push
```

푸시까지가 정리 작업의 끝이다. 커밋만 하고 멈추지 않는다.

---

## 실습 규칙

- 원본 실습 파일은 git에 커밋되어 있으므로 **제자리에서 수정**한다 (사본을 만들지 않는다).
- 요청받은 열/범위만 손댄다. 작업 중 발견한 다른 이상(중복 행, 결측치, 날짜 형식 혼재 등)은 **고치지 말고 보고만** 한다 — 강의가 이후 차시에서 다룰 소재일 수 있다.
- **없는 값을 지어내지 않는다.** 원본에 없으면 0이나 임의값으로 채우지 말고 빈칸으로 둔다.
- 수정 후에는 전수 검증하고, 무엇이 몇 건 바뀌었는지 표로 보고한다.
- `결과참고/`는 정답지다. 실습을 먼저 제 손으로 끝내기 전에 열어보지 않는다.

---

## 필기 규칙

- **차시 1개 = md 파일 1개.** `docs/0강.md` ~ `docs/8강.md`. 차시가 늘지 않는 한 파일을 새로 만들지 않는다.
- 파일명은 자료 폴더명(`0강`, `1강`, …)과 그대로 맞춘다.

한 차시 md는 보통 두 덩어리로 구성된다. 완성된 예시는 [docs/1강.md](docs/1강.md) 참고.

1. **강의 개념** — 사용자가 붙여넣는 강의 슬라이드 캡처를 표/구조로 옮긴다. 슬라이드에 있는 내용만 적고, 없는 걸 지어내지 않는다.
2. **실습** — 그 차시에 실제로 던진 프롬프트와 결과.

프롬프트를 받아 적을 때:

- 문장을 다듬거나 요약하지 말고 **받아쓴 그대로** 인용 블록에 넣는다. (오타·비문 포함)
- 결과는 무엇이 몇 건 바뀌었는지 표로 짧게. 길게 해설하지 않는다.
- 사용자가 프롬프트만 불러주고 정리를 지시하지 않았다면, 그때는 프롬프트만 쌓아두고 **먼저 나서서 필기를 재구성하지 않는다.**

---

## 커밋

형식은 `{이모지}{타입}: [{N}강] {내용}`. 한국어로 쓴다.

```
🎉begin: [0강] 프로젝트 시작 및 필기노트화 완료
🎨improve: [1강] 완료 : 및 필기노트화 완료
```

커밋 전 `git status`로 이번 차시 범위 밖의 변경이 섞여 있지 않은지 확인한다.
